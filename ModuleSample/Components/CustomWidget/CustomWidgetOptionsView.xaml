<UserControl x:Class="ModuleSample.Components.CustomWidget.CustomWidgetOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
  <Grid>
    <Button Background="{Binding BackgroundColor}" Foreground="{Binding ForegroundColor}" Content="Click!"
            Width="80" Height="30" Margin="16,12,0,4" HorizontalAlignment="Center" FontWeight="Bold"
            Command="{Binding ChangeButtonColorCommand}"/>
  </Grid>
</UserControl>

