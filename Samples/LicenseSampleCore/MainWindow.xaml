<Window x:Class="LicenseSampleCore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LicenseSampleCore"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
            <RowDefinition Height="5" />
            <RowDefinition Height="30" />
            <RowDefinition Height="5" />
            <RowDefinition Height="30" />
            <RowDefinition Height="5" />
        </Grid.RowDefinitions>

        <Button Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="3" IsEnabled="{Binding IsSdkEngineConnected}" Content="GET" Click="ButtonGetSpecific_Click" />
        <Button Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="5" IsEnabled="{Binding IsSdkEngineConnected}" Content="GET ALL LICENSES" Click="ButtonGetAll_Click" />
        
        <ListBox ItemsSource="{Binding LicenseTypes}" Grid.Column="1" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <CheckBox Content="{Binding Label}" IsChecked="{Binding Checked}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ListBox ItemsSource="{Binding LicenseResult}" Grid.Column="3" Grid.Row="1">
            <ListBox.ItemTemplate> 
                <DataTemplate> 
                    <StackPanel> 
                        <TextBlock Text="{Binding}" /> 
                    </StackPanel> 
                </DataTemplate> 
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Window>

